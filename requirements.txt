# ─────────────────────────────────────────────────────────────────
# SIKAP – requirements.txt
# Dioptimalkan untuk Streamlit Cloud (free tier)
# Python 3.11 | RAM limit ~1 GB
# ─────────────────────────────────────────────────────────────────

# Core web framework
streamlit>=1.35.0,<2.0.0

# Google Generative AI (Gemini + Embedding)
# Gunakan paket baru "google-genai" (bukan google-generativeai lama)
google-genai>=0.3.0

# Vector search – versi CPU-only agar ringan di cloud
faiss-cpu>=1.8.0

# Data processing
pandas>=2.1.0
numpy>=1.26.0

# Pickle sudah built-in Python, tidak perlu ditambah
